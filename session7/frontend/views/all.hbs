{{>header}}
{{>navbar}}
<table class="table table-bordered my-4">
    <thead>
        <tr>
            <th>id</th>
            <th>name</th>
            <th>age</th>
            <th>controls</th>
        </tr>
    </thead>

    <tbody>
        {{#if isEmpty}}
            <tr class="alert alert-danger text-center">
                <td colspan="4">No Users Yet</td>
            </tr>
        {{else}}
            {{#each allPlayers}}
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.name}}</td>
                <td>{{this.age}}</td>
                <td>
                    <a href="/single/{{this.id}}" class="btn btn-success mx-2">Show</a>
                    <a href="/edit/{{this.id}}" class="btn btn-warning mx-2">Edit</a>
                </td>
            </tr>
            {{/each}}
        {{/if}}
    </tbody>
</table>
{{>footer}}